[project]
name = "ejpipeline"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "coverage>=7.6.8",
    "ipython>=8.30.0",
    "pytest>=8.3.4",
    "ruff>=0.8.2",
]
